# Number of Buildings Facing the Sun

## Description

Given the array `arr[]` of heights of certain buildings that lie adjacent to each other, sunlight starts falling from the left side of the buildings. If there is a building of a certain height, all the buildings to the right side of it having lesser heights cannot see the sun. Find the total number of buildings that receive sunlight.

## Example

```
 input = [6, 2, 8, 4, 11, 13]
output:  4

 input = [2, 5, 1, 8, 3]
output = 3

 input = [0, 0, 1, 1, 2]
output = 3
```

## Placeholder

```kotlin
fun countVisibleBuildings(heights: IntArray): Int {
 // Fill here
}
```

## Solution

??? "Expand"

    ```kotlin
    fun countVisibleBuildings(heights: IntArray): Int {
      var maxHeight = -1
      var count = 0
      for (h in heights) {
        if (h > maxHeight) count++
        maxHeight = max(maxHeight, h)
      }
      return count
    }
    ```

## Unit tests

??? "Expand"

    ```kotlin
    package org.example

    import org.junit.jupiter.api.Assertions.assertEquals
    import org.junit.jupiter.api.Test

    class BuildingSunlightTest {

      @Test
      fun `test standard case with mixed heights`() {
        val heights = intArrayOf(6, 2, 8, 4, 11, 13)
        // Visible: 6, 8, 11, 13
        assertEquals(4, countVisibleBuildings(heights))
      }

      @Test
      fun `test small buildings hidden behind large ones`() {
        val heights = intArrayOf(2, 5, 1, 8, 3)
        // Visible: 2, 5, 8
        assertEquals(3, countVisibleBuildings(heights))
      }

      @Test
      fun `test strictly increasing heights`() {
        // Every building should be visible
        val heights = intArrayOf(1, 2, 3, 4, 5)
        assertEquals(5, countVisibleBuildings(heights))
      }

      @Test
      fun `test strictly decreasing heights`() {
        // Only the first building is visible
        val heights = intArrayOf(10, 8, 6, 4, 2)
        assertEquals(1, countVisibleBuildings(heights))
      }

      @Test
      fun `test identical heights`() {
        // Only the first one is visible because others are not strictly taller
        val heights = intArrayOf(5, 5, 5, 5)
        assertEquals(1, countVisibleBuildings(heights))
      }

      @Test
      fun `test single building`() {
        val heights = intArrayOf(10)
        assertEquals(1, countVisibleBuildings(heights))
      }

      @Test
      fun `test large gap between heights`() {
        val heights = intArrayOf(1, 100, 2, 200)
        // Visible: 1, 100, 200
        assertEquals(3, countVisibleBuildings(heights))
      }

      @Test
      fun `test buildings with zero height`() {
        val heights = intArrayOf(0, 0, 1, 1, 2)
        // First 0 is visible, 1 is visible, 2 is visible.
        // Note: Logic depends on if 0 is considered a building or if "greater than" is used.
        // Assuming strictly greater for subsequent buildings.
        assertEquals(3, countVisibleBuildings(heights))
      }
    }
    ```
